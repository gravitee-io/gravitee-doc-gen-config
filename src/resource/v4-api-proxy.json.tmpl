{
  "api": {
    "definitionVersion": "V4",
    "type": "PROXY",
    "name": "{{ .Title }} example API",
    "resources": [
      {
        "name": "{{ default .Properties.resourceName .ID }}",
        "type": "{{ .ID }}",
        "enabled": true,
        "configuration":
          {{ indent 10 .Node }}
      }
    ]
  }
}
